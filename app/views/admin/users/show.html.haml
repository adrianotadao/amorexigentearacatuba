#user.show
  %h1 Detalhes do usuário #{@user.name} 

  %div.group
    %p.active
      %b Ativo:
      = @user.status
    %p.name
      %b Nome:
      = @user.name

  .actions
    = link_to 'Editar', [:edit, :admin, @user], :class => 'button edit'
    = link_to 'Voltar', [:admin, :brands], :class => 'button back'
